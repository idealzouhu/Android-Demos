<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 1. 声明接收开机广播的权限 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" /> <!-- 从 Android 13 开始，发送通知需要声明权限 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Broadcastbasicjavaview">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 静态注册：开机广播接收器 -->
        <receiver
            android:name=".BootCompleteReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

<!--        &lt;!&ndash; 静态注册：有序广播的接收器（并设置优先级） &ndash;&gt;-->
<!--        <receiver-->
<!--            android:name=".HighPriorityReceiver"-->
<!--            android:enabled="true"-->
<!--            android:exported="false">-->
<!--            <intent-filter android:priority="100">-->
<!--                &lt;!&ndash; 优先级最高，最先接收 &ndash;&gt;-->
<!--                <action android:name="com.example.broadcast.basic.ACTION_ORDERED_BROADCAST" />-->
<!--            </intent-filter>-->
<!--        </receiver>-->
<!--        <receiver-->
<!--            android:name=".LowPriorityReceiver"-->
<!--            android:enabled="true"-->
<!--            android:exported="false">-->
<!--            <intent-filter android:priority="50">-->
<!--                &lt;!&ndash; 优先级较低，在高优先级之后接收 &ndash;&gt;-->
<!--                <action android:name="com.example.broadcast.basic.ACTION_ORDERED_BROADCAST" />-->
<!--            </intent-filter>-->
<!--        </receiver>-->
    </application>

</manifest>