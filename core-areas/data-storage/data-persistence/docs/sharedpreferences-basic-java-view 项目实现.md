## 项目概述

本案例演示了 Android 中 SharedPreferences 的基本使用方法，重点展示了如何保存和读取用户的配置信息，实现"记住密码"功能。



### 项目结构

```
sharedpreferences-basic-java-view/
├── 📱 app/
│   ├── src/main/
│   │   ├── java/com/example/sharedpreferences/basic/
│   │   │   ├── MainActivity.java                 # 主Activity
│   │   │   └── SettingsManager.java              # SharedPreferences封装类
│   │   └── res/
│   │       ├── layout/
│   │       │   └── activity_main.xml             # 主界面布局
│   │       └── values/
│   │           └── strings.xml                   # 字符串资源
│   └── build.gradle.kts
├── 📄 README.md
├── 📄 build.gradle.kts
└── 📄 settings.gradle.kts
```



## 核心功能点 

### 1. SharedPreferences 基本操作 

- 使用 `getSharedPreferences()` 获取实例
- -通过 `Editor` 进行数据修改 
- 使用 `apply()` 和 `commit()` 提交更改 

### 2. 数据封装模式 
- 创建专门的 `SettingsManager` 类管理所有配置操作 
- 使用常量定义键名，避免硬编码 
- 提供统一的数据访问接口 

### 3. 配置信息管理 
- 用户名和密码的保存与读取 
- "记住密码"选项的状态管理
- 数据清除功能