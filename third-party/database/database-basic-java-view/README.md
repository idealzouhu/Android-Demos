### 项目概述

本案例演示了 Android 中 SQLite 数据库的基础使用方法，重点展示了数据库的创建、表的建立以及增删改查（CRUD）等基本操作。
同时，项目采用模块化设计，包含两个独立的数据库版本，便于开发者理解和测试数据库升级过程。
| 特性             | 版本1 (V1)    | 版本2 (V2)                         |
| :--------------- | :------------ | :--------------------------------- |
| **数据库版本号** | 1             | 2                                  |
| **学生表字段**   | id, name, age | id, name, age, email, created_time |
| **班级表**       | 无            | 有                                 |
| **升级逻辑**     | 基础版本      | 支持从版本1升级                    |



### 项目结构

```
database-basic-java-view/
├── 📱 app/                           # 主应用模块
│   ├── src/main/java/com/example/database/basic/
│   │   └── MainActivity.java          # 主Activity，包含界面逻辑(暂使用版本1实体)
│   ├── src/main/res/layout/
│   │   ├── activity_main.xml          # 主界面布局
│   │   └── item_student.xml           # 列表项布局
│   └── build.gradle
├── 📚 database-v1/                   # 数据库版本1实现 - 基础功能
│   ├── src/main/java/com/example/database/v1/
│   │   ├── DatabaseHelper.java       # 版本1数据库辅助类
│   │   ├── StudentDao.java           # 版本1数据访问对象
│   │   └── Student.java              # 版本1实体类 (id, name, age)
│   └── build.gradle
├── 📚 database-v2/                   # 数据库版本2实现 - 增强功能  
│   ├── src/main/java/com/example/database/v2/
│   │   ├── DatabaseHelper.java       # 版本2数据库辅助类（含完整升级逻辑）
│   │   ├── StudentDao.java           # 版本2数据访问对象（支持新字段）
│   │   └── Student.java              # 版本2实体类 (新增email, created_time)
│   └── build.gradle
├── 📄 build.gradle                   # 项目级依赖配置
├── 📄 settings.gradle                # 模块包含配置
└── 📄 README.md                      # 项目文档
```

### 学习目标

通过该项目，你将掌握：

- 简单CRUD：基础的增删改查操作
- 高级查询：支持按邮箱模糊查询、年龄范围查询等功能
- 完整升级逻辑：处理从v1到v2的数据库迁移
